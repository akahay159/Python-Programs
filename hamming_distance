def hamming(x,y):
    xx=bin(x)
    result=0
    for i in range(len(xx)):
        if((x&(1<<i)^y&(1<<i))!=0):
            result+=1
    return result
print(hamming(1,2))
#code is given in Hackerrank you need to search for hamming distance
